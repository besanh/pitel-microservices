package service

import (
	"context"

	"github.com/tel4vn/fins-microservices/model"
)

func (s *Message) uploadDoc(ctx context.Context, authUser *model.AuthUser, data model.MessageRequest) (fileUrl string, err error) {
	return
}
